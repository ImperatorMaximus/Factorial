#include <stdio.h>

long fact(int n);
int main() {
	int b;
	printf("Ingrese el número deseado\n");
	scanf("%d",&b);
	printf("El factorial es %ld\n", fact(b));
	return 0;
}
long fact(int n){
    if(n==0)
        return 1;
    else
        return fact(n-1)*n;
}
